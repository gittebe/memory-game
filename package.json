{
  "name": "memory-game",
  "version": "1.0.0",
  "description": "This is a classic Memory Game built with React using useReducer for state management. The project is divided into multiple branches so we can work in parallel without interfering with each other’s code.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.2"
  }
}
